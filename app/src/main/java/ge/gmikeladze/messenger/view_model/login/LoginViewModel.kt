package ge.gmikeladze.messenger.view_model.login

import android.content.SharedPreferences
import androidx.lifecycle.ViewModel

class LoginViewModel(private val preferences: SharedPreferences) : ViewModel() {
}